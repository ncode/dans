apiVersion: v1
kind: Namespace
metadata:
  name: dans
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: dans
  namespace: dans
automountServiceAccountToken: false
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dans
  namespace: dans
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: dans
  template:
    metadata:
      labels:
        app.kubernetes.io/name: dans
    spec:
      serviceAccountName: dans
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: dans
          image: ghcr.io/ncode/dans:RELEASE
          imagePullPolicy: IfNotPresent
          args: ["serve"]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: DANS_LISTEN
              value: "0.0.0.0:8080"
            - name: DANS_DATABASE_URL_FILE
              value: /run/secrets/dans/database-url
            - name: DANS_POWERDNS_URL
              value: http://powerdns-api.powerdns.svc.cluster.local:8081
            - name: DANS_POWERDNS_UPSTREAM
              value: powerdns
            - name: DANS_POWERDNS_API_KEY_FILE
              value: /run/secrets/dans/powerdns-api-key
          ports:
            - name: http
              containerPort: 8080
          livenessProbe:
            httpGet:
              path: /livez
              port: http
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 256Mi
          volumeMounts:
            - name: runtime-secrets
              mountPath: /run/secrets/dans
              readOnly: true
      volumes:
        - name: runtime-secrets
          secret:
            secretName: dans-runtime
            defaultMode: 0440
---
apiVersion: v1
kind: Service
metadata:
  name: dans
  namespace: dans
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dans
  ports:
    - name: http
      port: 8080
      targetPort: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dans
  namespace: dans
spec:
  ingressClassName: trusted
  tls:
    - hosts: ["dans.example.com"]
      secretName: dans-tls
  rules:
    - host: dans.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: dans
                port:
                  name: http
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: dans-private-boundary
  namespace: dans
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: dans
  policyTypes: ["Ingress", "Egress"]
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              networking.dans.io/trusted-ingress: "true"
          podSelector:
            matchLabels:
              app.kubernetes.io/component: controller
      ports:
        - protocol: TCP
          port: 8080
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    - to:
        - namespaceSelector:
            matchLabels:
              networking.dans.io/postgresql: "true"
          podSelector:
            matchLabels:
              app.kubernetes.io/name: postgresql
      ports:
        - protocol: TCP
          port: 5432
    - to:
        - namespaceSelector:
            matchLabels:
              networking.dans.io/powerdns: "true"
          podSelector:
            matchLabels:
              app.kubernetes.io/name: powerdns
      ports:
        - protocol: TCP
          port: 8081
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: powerdns-api-from-dans-only
  namespace: powerdns
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: powerdns
  policyTypes: ["Ingress"]
  ingress:
    - ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: dans
          podSelector:
            matchLabels:
              app.kubernetes.io/name: dans
      ports:
        - protocol: TCP
          port: 8081
